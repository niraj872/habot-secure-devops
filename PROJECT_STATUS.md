# Final Project Status

## Implemented

- Secure D0 Raw Landing Google Cloud Storage bucket.
- Customer-managed encryption key and key rotation.
- Uniform bucket-level access and public-access prevention.
- Versioning and lifecycle controls.
- D1 Staged/Enforced BigQuery dataset and explicit student onboarding schema.
- BigQuery table IAM and row-level security.
- Separate workload identities for deployment, data pipeline, backend, analytics, and streaming delivery.
- Dedicated Terraform deployment role.
- GitHub OpenID Connect Workload Identity Federation with repository and reference restrictions.
- Pub/Sub onboarding topic, BigQuery streaming sink, and dead-letter topic.
- Remote Terraform state bootstrap.
- App Engine Terraform provisioning capability.
- Django REST Framework serializer, deterministic binary Yes/No logic, migration, and tests.
- JSON Schema and spreadsheet mapping with Wrap Text enabled.
- Fail-Closed GitHub Actions gates and protected deployment workflow.
- Fifteen-slide presentation.

## Environment-specific actions before submission

The candidate must run the bootstrap and main Terraform stack in the candidate's own Google Cloud project, configure GitHub repository variables and secrets, and capture live evidence from the actual environment.

The supplied assignment source did not contain the candidate's email address or phone number. Those real contact details must be added to the submission header and code/document headers before the final handoff.
